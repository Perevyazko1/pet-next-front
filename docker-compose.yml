version: '3.8'

services:
  nextjs:
    build: .
    container_name: nextjs_app
    ports:
      - "3000:3000"  # Порт для обращения к контейнеру

    volumes:
      - ./src:/app/src
    environment:
      - NODE_ENV=production



